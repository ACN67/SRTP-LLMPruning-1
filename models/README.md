# Local models

This directory is a local mount or storage location for model weights. Everything here except this README is ignored by Git. On GPU servers, prefer mounting persistent storage at `/data/models`.
